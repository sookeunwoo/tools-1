/**
 * 서비스 기본값 — **의존성 없는 잎 모듈**이다.
 *
 * 이 파일이 따로 있는 이유는 값 하나 때문이 아니라 프로세스 크기 때문이다.
 * MCP 어댑터와 CLI는 세션·명령마다 새로 뜨는데, 이 상수를 api.ts에서 가져오면
 * 캐시·정책·sops·YAML 파서까지 통째로 딸려 들어온다 — 한 줄도 실행하지 않으면서
 * 파싱 비용과 힙을 그대로 문다. 잎 모듈로 떼어 두면 그 연결이 끊긴다.
 */

/** env 파라미터 생략 시 기본값. prod 조회는 항상 명시적이어야 한다 (스펙 §4.2). */
export const DEFAULT_ENV = 'dev';
